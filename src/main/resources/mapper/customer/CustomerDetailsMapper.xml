<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.crown.mapper.customer.CustomerDetailsMapper">



    <select id="queryByCid" resultType="CustomerDetails">
        select
         id,nick_name,c_id,name,phone,sex,address,create_time,update_time
         from customer_details where c_id = #{id} limit 0,1;
    </select>
</mapper>
